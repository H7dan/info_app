name: Validate content

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate handbook content
        shell: pwsh
        run: ./tools/validate_content.ps1
